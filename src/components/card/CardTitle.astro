---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'h3'> {}

const { class: className, ...restProps } = Astro.props
---

<h3
  {...restProps}
  class:list={['font-semibold leading-none tracking-tight', className]}
>
  <slot />
</h3>
